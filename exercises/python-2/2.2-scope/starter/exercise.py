# Scope Practice Exercise

# ============================================================================
# Task 1: Local vs Global Scope
# ============================================================================
# Write a function called `demo_scope()` that:
# - Creates a local variable called `local_message` with the value "I'm local"
# - Accesses the global variable `global_message` defined below
# - Prints both messages
# After defining the function, call it to demonstrate local and global scope.

global_message = "I'm global"

# Write your code here


# ============================================================================
# Task 2: Modifying Global Variables
# ============================================================================
# Create a global variable called `counter` with an initial value of 0.
# Write a function called `increment_counter()` that:
# - Uses the `global` keyword to modify the global `counter` variable
# - Increments it by 1
# Call the function multiple times and print the counter value to show it's changing.

# Write your code here


# ============================================================================
# Task 3: Nested Function Scope (Enclosing Scope)
# ============================================================================
# Write a function called `outer_function()` that:
# - Creates a variable called `outer_var` with the value "from outer"
# - Defines an inner function called `inner_function()` that:
#   - Creates a variable called `inner_var` with the value "from inner"
#   - Prints both `outer_var` and `inner_var`
# - Calls the inner function
# Call `outer_function()` to demonstrate enclosing scope.

# Write your code here


# ============================================================================
# Task 4: Scope Rules with Variable Shadowing
# ============================================================================
# Create a global variable called `name` with the value "Global"
# Write a function called `shadow_demo()` that:
# - Creates a local variable also called `name` with the value "Local"
# - Prints the local `name`
# After the function, print the global `name` to show it wasn't changed.

# Write your code here

