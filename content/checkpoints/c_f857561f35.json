{
  "id": "c_f857561f35",
  "item_id": "i_8e707dabda",
  "course_id": "web-dev-2",
  "legacy_path": null,
  "exercise": "web-dev-2/2.4-arrays",
  "title": "JavaScript Arrays",
  "preset": "github-exercise",
  "requires_sign_off": false,
  "fields": [
    {
      "id": "repo_url",
      "type": "url",
      "label": "Link to your GitHub repository",
      "required": true,
      "help": "It looks like https://github.com/your-username/your-repository"
    },
    {
      "id": "notes",
      "type": "longText",
      "label": "Anything you want your coach to know?",
      "required": false
    }
  ],
  "required_one_of": [],
  "instructions_html": "<h2>JavaScript Arrays</h2>\n<p>An <strong>array</strong> is a type of data structure in JavaScript used to <strong>store a collection of items</strong>.<br>Arrays are:</p>\n<ul>\n<li><strong>Ordered</strong> – items have a specific position (index)</li>\n<li><strong>Mutable</strong> – items can be changed</li>\n<li><strong>Allow duplicates</strong></li>\n</ul>\n<hr>\n<h3>Creating an Array</h3>\n<pre><code class=\"language-javascript\">let fruits = ['apple', 'banana', 'cherry'];\n</code></pre>\n<hr>\n<h3>Accessing Items in an Array</h3>\n<p>Array items are accessed using <strong>index numbers</strong>, starting at <strong>0</strong>.</p>\n<pre><code class=\"language-javascript\">console.log(fruits[0]);  // 'apple'\nconsole.log(fruits[1]);  // 'banana'\nconsole.log(fruits[fruits.length - 1]); // 'cherry' (last item)\n</code></pre>\n<hr>\n<h3>Modifying an Array</h3>\n<p>You can change a specific item by assigning a new value to an index.</p>\n<pre><code class=\"language-javascript\">fruits[1] = 'orange';\nconsole.log(fruits); // ['apple', 'orange', 'cherry']\n</code></pre>\n<hr>\n<h3>Looping Through an Array</h3>\n<p>You can loop through arrays using several methods.</p>\n<h4>1. Traditional <code>for</code> Loop</h4>\n<pre><code class=\"language-javascript\">for (let i = 0; i &lt; fruits.length; i++) {\n    console.log(fruits[i]);\n}\n</code></pre>\n<h4>2. <code>for...of</code> Loop (cleaner)</h4>\n<pre><code class=\"language-javascript\">for (let fruit of fruits) {\n    console.log(fruit);\n}\n</code></pre>\n<hr>\n<h3>Checking if an Item Exists</h3>\n<p>Use the <code>.includes()</code> method to see if an element is in the array.</p>\n<pre><code class=\"language-javascript\">if (fruits.includes('apple')) {\n    console.log('Yes, apple is in the fruits array.');\n}\n</code></pre>\n<hr>\n<h3>Adding and Removing Items</h3>\n<h4>Add to the End</h4>\n<pre><code class=\"language-javascript\">fruits.push('banana');\nconsole.log(fruits); // ['apple', 'orange', 'cherry', 'banana']\n</code></pre>\n<h4>Remove from the End</h4>\n<pre><code class=\"language-javascript\">fruits.pop();\nconsole.log(fruits); // ['apple', 'orange', 'cherry']\n</code></pre>\n<h4>Add to the Beginning</h4>\n<pre><code class=\"language-javascript\">fruits.unshift('kiwi');\nconsole.log(fruits); // ['kiwi', 'apple', 'orange', 'cherry']\n</code></pre>\n<h4>Remove from the Beginning</h4>\n<pre><code class=\"language-javascript\">fruits.shift();\nconsole.log(fruits); // ['apple', 'orange', 'cherry']\n</code></pre>\n<hr>\n<h3>Removing a Specific Item</h3>\n<p>Use <code>.indexOf()</code> to find the index of an item, and <code>.splice()</code> to remove it.</p>\n<pre><code class=\"language-javascript\">let index = fruits.indexOf('cherry');\nif (index !== -1) {\n    fruits.splice(index, 1);\n}\nconsole.log(fruits); // ['apple', 'orange']\n</code></pre>\n<hr>\n<h3>Sorting and Reversing</h3>\n<h4>Sort Alphabetically</h4>\n<pre><code class=\"language-javascript\">fruits.sort();\nconsole.log(fruits); // ['apple', 'orange']\n</code></pre>\n<h4>Reverse Order</h4>\n<pre><code class=\"language-javascript\">fruits.reverse();\nconsole.log(fruits); // ['orange', 'apple']\n</code></pre>\n<hr>\n<h3>Slicing an Array</h3>\n<p><code>.slice()</code> creates a <strong>copy</strong> of a section of an array without changing the original.</p>\n<pre><code class=\"language-javascript\">let numbers = [0, 1, 2, 3, 4, 5];\n\nlet slice1 = numbers.slice(2, 4); // [2, 3]\nlet slice2 = numbers.slice(0, 3); // [0, 1, 2]\nlet slice3 = numbers.slice(3);    // [3, 4, 5]\n\nconsole.log(slice1);\nconsole.log(slice2);\nconsole.log(slice3);\n</code></pre>\n<hr>\n<h3>Summary</h3>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Description</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>push()</code></td>\n<td>Add to the end</td>\n<td><code>arr.push('x')</code></td>\n</tr>\n<tr>\n<td><code>pop()</code></td>\n<td>Remove from the end</td>\n<td><code>arr.pop()</code></td>\n</tr>\n<tr>\n<td><code>unshift()</code></td>\n<td>Add to the beginning</td>\n<td><code>arr.unshift('x')</code></td>\n</tr>\n<tr>\n<td><code>shift()</code></td>\n<td>Remove from the beginning</td>\n<td><code>arr.shift()</code></td>\n</tr>\n<tr>\n<td><code>indexOf()</code></td>\n<td>Find position of an element</td>\n<td><code>arr.indexOf('x')</code></td>\n</tr>\n<tr>\n<td><code>splice()</code></td>\n<td>Remove or replace elements</td>\n<td><code>arr.splice(2, 1)</code></td>\n</tr>\n<tr>\n<td><code>slice()</code></td>\n<td>Copy a portion of an array</td>\n<td><code>arr.slice(1, 3)</code></td>\n</tr>\n<tr>\n<td><code>sort()</code></td>\n<td>Sort items</td>\n<td><code>arr.sort()</code></td>\n</tr>\n<tr>\n<td><code>reverse()</code></td>\n<td>Reverse the array</td>\n<td><code>arr.reverse()</code></td>\n</tr>\n<tr>\n<td><code>includes()</code></td>\n<td>Check if item exists</td>\n<td><code>arr.includes('x')</code></td>\n</tr>\n</tbody></table>\n<hr>\n<h4>Key Takeaways</h4>\n<ul>\n<li>Arrays are ordered lists that can hold multiple data types.</li>\n<li>Use indexes to access or modify elements.</li>\n<li>Use array methods to add, remove, or rearrange items.</li>\n<li><code>slice()</code> creates copies; <code>splice()</code> changes the original array.</li>\n</ul>\n<h2>Hand it in</h2>\n<ol>\n<li>Download the starter files from this page and unzip them into a new folder.</li>\n<li>Make a new repository on GitHub and push your folder to it. (GitHub Basics, the first exercise in Python I, walks through this.)</li>\n<li>Run the tests yourself with <code>npm test</code>. When you push, GitHub runs them too: a green check next to your latest commit means they pass, a red X means something still needs work.</li>\n<li>Submit the link to your repository with the form below.</li>\n</ol>",
  "starter_path": "exercises/web-dev-2/2.4-arrays/starter",
  "grading_hint": {
    "runner": "jest",
    "tests": [
      "exercises/web-dev-2/2.4-arrays/starter/__tests__/learn.test.js"
    ]
  }
}
