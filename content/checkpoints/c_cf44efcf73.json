{
  "id": "c_cf44efcf73",
  "item_id": "i_2ff7fd241b",
  "course_id": "python-1",
  "legacy_path": null,
  "exercise": "python-1/1.5-input",
  "title": "User Input",
  "preset": "github-exercise",
  "requires_sign_off": false,
  "fields": [
    {
      "id": "repo_url",
      "type": "url",
      "label": "Link to your GitHub repository",
      "required": true,
      "help": "It looks like https://github.com/your-username/your-repository"
    },
    {
      "id": "notes",
      "type": "longText",
      "label": "Anything you want your coach to know?",
      "required": false
    }
  ],
  "required_one_of": [],
  "instructions_html": "<h2>User Input</h2>\n<p>The <code>input()</code> function allows the user to input a value from the command line (also called the terminal).<br>The value is stored as a <strong>string</strong> by default, so you might need to convert it to another data type.</p>\n<pre><code class=\"language-python\">name = input(\"What's your name? \")\nage = input(\"How old are you? \")\n</code></pre>\n<p>You can pass multiple arguments to a <code>print()</code> function or use <code>+</code> to concatenate (combine) them together.</p>\n<pre><code class=\"language-python\">print(\"Hello, \" + name + \", you are \" + age + \" years old.\")\n</code></pre>\n<p>This method also works — the arguments will be separated by a space by default.</p>\n<pre><code class=\"language-python\">print(\"My friend\", name, \"is\", age, \"years old.\")\n</code></pre>\n<hr>\n<p>Let's get a little more advanced.<br>We’re going to ask the user their <strong>birth year</strong>, and then tell them how old they are turning this year (2023).</p>\n<p>To do this, we'll need to use <strong>type conversion</strong> functions, specifically <code>int()</code> and <code>str()</code>, to change variables from one type to another.</p>\n<pre><code class=\"language-python\">birth_year = input(\"What year were you born in? \")\nage = 2023 - int(birth_year)\nprint(\"You are turning \" + str(age) + \" years old this year!\")\n</code></pre>\n<hr>\n<p>In the code above:</p>\n<ul>\n<li>We first get the user's birth year. Since <code>input()</code> always returns a <strong>string</strong>, we use <code>int()</code> to convert <code>birth_year</code> to an <strong>integer</strong> before we can use it in a calculation.<br>You can't subtract a word from a number!  </li>\n<li>We then calculate the user's age by subtracting their birth year from <code>2023</code>, and store the result in the variable <code>age</code>.  </li>\n<li>Finally, we use the <code>str()</code> function to convert <code>age</code> back to a <strong>string</strong> so we can concatenate it with the other strings in our <code>print()</code> statement.</li>\n</ul>\n<h2>Hand it in</h2>\n<ol>\n<li>Download the starter files from this page and unzip them into a new folder.</li>\n<li>Make a new repository on GitHub and push your folder to it. (GitHub Basics, the first exercise in Python I, walks through this.)</li>\n<li>Run the tests yourself with <code>pytest</code>. When you push, GitHub runs them too: a green check next to your latest commit means they pass, a red X means something still needs work.</li>\n<li>Submit the link to your repository with the form below.</li>\n</ol>",
  "starter_path": "exercises/python-1/1.5-input/starter",
  "grading_hint": {
    "runner": "pytest",
    "tests": [
      "exercises/python-1/1.5-input/starter/tests/test_exercise.py"
    ]
  }
}
